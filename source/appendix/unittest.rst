########################################
Tester une fonction comme un pro
########################################

Python met à disposition le module ``unittest`` pour tester des fonctions (ou de
classes). Voici un exemple d'utilisation de ce module

.. literalinclude:: scripts/merge_sort_2_test.py
   :language: python
   :linenos:

Il suffit ensuite d'exécuter ce script Python pour pouvoir tester la fonction
``merge_sort`` définie dans le module ``merge_sort_2``.
